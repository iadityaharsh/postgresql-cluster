# Pinned versions for binaries we install from upstream releases.
# Sourced by scripts/01-install-packages.sh and update.sh.
# Bumping any value here is the single point of truth — re-test
# cluster bootstrap after changing.

ETCD_VERSION="3.5.21"
